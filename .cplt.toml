# Generated by `cplt init` — review before trusting.
# Detected: Node.js, Docker, Environment secrets, Vite

[deny]
env = ["UNLEASH_SERVER_API_TOKEN"]

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: (root)
allow_docker = true
# Suggested by: (root)
allow_localhost_any = true

[propose.allow]
localhost = [5173]

